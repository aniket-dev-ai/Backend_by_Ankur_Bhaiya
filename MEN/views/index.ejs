<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-center p-6"></body>
    <h1 class="text-4xl font-bold text-green-500 mb-8">Hello World</h1>
    <div class="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">
        <% data.forEach(item => { %>
            <div class="bg-white p-6 rounded-lg shadow-md hover:shadow-lg   transition duration-300">
                <h2 class="text-2xl font-semibold text-red-400 mb-2"><%= item.name %></h2>
                <p class="text-lg mb-1 text-green-400"><%= item.number %></p>
                <p class="text-lg mb-1 text-purple-400"><%= item.email %></p>
                <p class="text-xl text-blue-500"><%= item.address %></p>
            </div>
        <% }) %>
    </div>
</body>
</html>